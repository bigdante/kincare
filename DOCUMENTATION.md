# 家庭健康中心 (Family Health Hub) - 项目文档

## 概述
“家庭健康中心”是一款专为家庭设计的、由 AI 驱动的综合健康管理应用。它采用统一的用户角色体系，允许任何用户管理自己的健康档案，或在获得授权的情况下代为管理他人的档案。该应用利用先进的 AI 能力提供个性化的健康建议、分析医疗单据并生成语音提醒。

## 核心功能

### 1. 统一角色与档案管理
- **无差别角色**：取消了固定的“管理者”和“被管理者”划分。每个用户既可以管理自己，也可以管理他人。
- **管理委派与授权**：
  - 用户可以将自己或所管理成员的档案委派给他人管理。
  - 非本人档案的委派需经过被管理者的显式授权同意。
  - 支持查看待处理的授权请求。
- **成员切换器**：在不同功能（时间线、建议、用药）中无缝切换家庭成员。
- **健康标签**：分配健康标签（例如“高血压”、“花生过敏”）并设置严重程度。
- **二维码分享**：生成档案专属二维码，他人扫描后可申请管理权限或获取档案信息。

### 2. AI 驱动的健康时间线
- **事件追踪**：记录健康事件，如就医、症状或日常备注。
- **AI 分析**：自动分析自由文本输入，提取结构化数据，包括日期、事件类型和相关的健康标签。

### 3. 每日建议与干预
- **个性化推荐**：根据成员的具体健康档案、年龄、BMI 和活跃的健康标签，生成每日饮食和运动建议。
- **条件化提醒**：仅当档案处于“被管理”或“自我管理”状态时，才会显示专门的用药/就诊提醒标签页。

### 4. 智能用药管理
- **详细排期**：记录药物名称、剂量、具体时间、药物总量和每日服用次数。
- **时间限制打卡**：用户只能为当前或过去的时间点进行服药打卡，未来时间的打卡将被拦截。
- **AI 语音提醒**：
  - AI 自动根据药品名称、剂量和时间生成温馨的语音提醒文案。
  - 支持在打卡时播放 AI 生成的语音。
- **AI 处方扫描**：上传处方单/药盒照片，AI 自动提取信息并填充用药表单。

### 5. 多端同步 (React & UniApp)
- **React Web 端**：提供完整的管理和分析功能，适合大屏操作。
- **UniApp 移动端**：适配微信小程序、App 和 H5，提供便捷的打卡和提醒功能，界面针对老年人进行了适老化优化（如巨型打卡按钮）。

## 技术实现

### 技术栈
- **前端 (Web)**：React 18 (Vite) + Tailwind CSS
- **前端 (移动端)**：UniApp (Vue 3)
- **状态管理**：React Context (Web) / Vue Reactive (Mobile)
- **AI 集成**：`@google/genai` (Gemini 3.1 Pro / Flash / TTS)
- **动画**：`motion/react` (Web) / CSS Transitions (Mobile)

### 核心逻辑
- **Store (`store.tsx`)**：集中式状态管理，处理档案委派 (`delegateManagement`)、授权接受 (`acceptManagement`) 和服药逻辑。
- **AI 服务 (`ai_service.ts` / `ai_service.js`)**：封装了所有 AI 调用，包括健康建议生成和用药语音文案生成。

### 数据安全与隐私
- **授权机制**：所有跨用户的档案访问必须经过被管理者的授权确认。
- **数据隔离**：确保用户只能访问其拥有权限或已获授权的档案数据。
�视化随时间变化的健康趋势（如血压、血糖）。
- **PDF 导出**：将当前的健康档案和记录导出为 PDF 文档，方便与医疗服务提供者共享。
- **标签禁忌 AI**：根据分配的健康标签，自动生成禁忌建议（注意事项）。

### 6. 基于角色的仪表盘
- **管理者视图**：拥有添加、编辑和管理档案、药物和事件的完全访问权限。
- **被关爱者视图**：为被关爱者量身定制的极简、只读仪表盘，用于查看他们的每日日程、播放语音提醒并查看个性化建议。

## UI / UX 设计

- **移动端优先**：主要针对移动设备设计，界面简洁、触控友好。
- **色彩搭配**：使用令人平静的、以健康为导向的颜色（翠绿色、柔和的灰色和蓝色），并使用语义颜色表示警报（红色表示危险，琥珀色表示警告）。
- **动画效果**：利用 `framer-motion` 实现平滑的页面过渡、模态弹窗和交互元素，提升整体用户体验。
- **无障碍设计**：清晰的排版、高对比度和直观的图标 (`lucide-react`)。

## 技术实现

### 技术栈
- **前端框架**：React 18 (Vite)
- **样式**：Tailwind CSS
- **状态管理与后端**：React Context 结合 Firebase Firestore 进行实时数据同步。
- **图标**：`lucide-react`
- **动画**：`motion/react`
- **AI 集成**：`@google/genai` (Gemini 3.1 Pro / Flash)

### 架构
- **Store (`store.tsx`)**：集中式状态管理，保存档案、事件、药物和日志。它还封装了所有异步 AI 调用和 Firebase 数据库的读写操作。
- **组件**：模块化的 React 组件（`Timeline`、`MedicationList`、`ProfileView` 等），它们消费 store 并处理 UI 逻辑。
- **国际化 (`i18n.ts`)**：支持中英双语的简单字典式国际化系统。

### AI 集成详情
该应用在几个核心功能上严重依赖 Gemini API：
- `analyzeAndAddEvent`：使用 `gemini-3.1-pro-preview` 结合 `responseSchema` 从非结构化文本中提取结构化 JSON。
- `generateInterventions`：使用 `gemini-3.1-pro-preview` 根据用户上下文生成个性化的健康建议。
- `analyzePrescriptionImage`：使用 `gemini-3.1-pro-preview`（多模态）读取多张图片中的文本并将其映射到药物数据结构。
- `generateTagAvoidance`：使用 `gemini-3.1-pro-preview` 为健康标签提供医疗背景和禁忌。
- `generateTTS`：使用 `gemini-2.5-flash-preview-tts` 将文本转换为语音音频（base64 编码）。

### 数据持久化
所有用户数据（包括档案、事件、用药计划、打卡记录、语音和每日建议）均实时同步并安全地保存在 Firebase Firestore 云端数据库中。
