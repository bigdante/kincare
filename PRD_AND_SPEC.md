# 亲安 (KinCare) 智能家庭健康管理系统
## 完整产品需求规格说明书 (PRD & Detailed Functional Spec)
**版本号**：v2.2-Final | **适用端**：Web Responsive / PWA / Android & iOS App (Capacitor / uni-app)  
**文档目标**：为前端、后端、测试及全栈研发人员提供像素级、字段级、交互事件级与状态机级的权威开发规范。

---

## 目录
1. [系统整体架构与全局规范](#一系统整体架构与全局规范)
2. [全局数据模型与字段字典 (Data Schema)](#二全局数据模型与字段字典-data-schema)
3. [全局公共组件与框架交互规范](#三全局公共组件与框架交互规范)
4. [Tab 1：健康打卡页 (HomeTab) 详细规格](#四tab-1健康打卡页-hometab-详细规格)
5. [Tab 2：健康时间轴页 (TimelineTab) 详细规格](#五tab-2健康时间轴页-timelinetab-详细规格)
6. [Tab 3：用药管理页 (MedsTab) 详细规格](#六tab-3用药管理页-medstab-详细规格)
   - [3.1 用药日历视图](#61-用药日历视图-calendar)
   - [3.2 智能家庭药箱视图](#62-智能家庭药箱视图-cabinet)
   - [3.3 服药日志与历史方案视图](#63-服药日志与历史方案视图-history)
   - [3.4 用药计划录入/编辑弹窗 (MedicationPlanModal)](#64-用药计划录入编辑弹窗-medicationplanmodal)
   - [3.5 药箱药品录入/编辑弹窗 (MedicineCabinetModal)](#65-药箱药品录入编辑弹窗-medicinecabinetmodal)
   - [3.6 到点服药测试闹钟仿真弹窗 (TestAlarmModal)](#66-到点服药测试闹钟仿真弹窗-testalarmmodal)
   - [3.7 服药打卡记录补录/修改弹窗 (MedicationLogEditModal)](#67-服药打卡记录补录修改弹窗-medicationlogeditmodal)
7. [Tab 4：健康数据全景页 (HealthTab) 详细规格](#七tab-4健康数据全景页-healthtab-详细规格)
   - [4.1 健康名片与 BMI 智能圆环](#71-健康名片与-bmi-智能圆环)
   - [4.2 重点指标概览面板](#72-重点指标概览面板)
   - [4.3 健康趋势图表 (HealthTrendChart)](#73-健康趋势图表-healthtrendchart)
   - [4.4 7 大分类人体全景指标折叠库](#74-7-大分类人体全景指标折叠库)
   - [4.5 指标录入与修改弹窗 (HealthLogModal / EditHealthRecordModal)](#75-指标录入与修改弹窗-healthlogmodal--edithealthrecordmodal)
   - [4.6 多维指标高级筛选抽屉](#76-多维指标高级筛选抽屉)
   - [4.7 健康标签云系统](#77-健康标签云系统)
   - [4.8 综合病历档案系统 (MedicalRecordModal - 6大模板自适应)](#78-综合病历档案系统-medicalrecordmodal---6大模板自适应)
   - [4.9 病历高保真详情与复查打卡抽屉 (MedicalRecordDetailModal)](#79-病历高保真详情与复查打卡抽屉-medicalrecorddetailmodal)
8. [Tab 5：个人与系统设置页 (MyTab) 详细规格](#八tab-5个人与系统设置页-mytab-详细规格)
   - [5.1 用户信息与编辑](#81-用户信息与编辑)
   - [5.2 家庭成员全权管理与编辑 (MemberEditModal)](#82-家庭成员全权管理与编辑-membereditmodal)
   - [5.3 AI 服务连接配置与连通性测试](#83-ai-服务连接配置与连通性测试)
   - [5.4 语音播报调试引擎](#84-语音播报调试引擎)
   - [5.5 全量健康数据备份与导出 (JSON / CSV)](#85-全量健康数据备份与导出-json--csv)
   - [5.6 多语言、账号安全与退出](#86-多语言账号安全与退出)
9. [全局危险操作二次确认机制 (8.0 规范)](#九全局危险操作二次确认机制-80-规范)
10. [长辈适老模式 (Senior Mode) 渲染规范](#十长辈适老模式-senior-mode-渲染规范)

---

## 一、系统整体架构与全局规范

### 1.1 设计系统与色彩 Token (Design Tokens)
- **品牌主色 (Primary)**：`#0D9488` (Teal 600)，用于主按钮、导航激活态、完成态进度、关键强调。
- **品牌强调色 (Primary Dark)**：`#0F766E` (Teal 700)，用于按下态、边框高亮。
- **品牌浅底色 (Primary Light)**：`#CCFBF1` (Teal 100) / `#F0FDFA` (Teal 50)，用于选中背景、标签浅底。
- **身份标识色 (Emerald)**：`#059669` (Emerald 600)，专用于成员切换轨选中头像边框与外发光。
- **语义警示色 (Warning)**：`#F59E0B` (Amber 500) / `#FEF3C7` (Amber 100)，用于待办、低库存告警、服药间隔警示。
- **语义危险色 (Danger / Error)**：`#EF4444` (Red 500) / `#FEE2E2` (Red 100)，用于漏服、指标严重异常、删除确认按钮。
- **语义信息色 (Info)**：`#3B82F6` (Blue 500) / `#EFF6FF` (Blue 50)，用于医疗就诊、PDF 报告标签。
- **中性色阶**：
  - 背景底色 (Background)：`#F9FAFB`
  - 卡片表面 (Surface)：`#FFFFFF`
  - 主标题/重点文字 (Neutral 900)：`#111827`
  - 正文/药名/表单输入 (Neutral 700)：`#374151`
  - 次要说明/时间戳 (Neutral 500)：`#6B7280`
  - 占位符/失效禁用 (Neutral 400)：`#9CA3AF`
  - 边框与分割线 (Neutral 200)：`#E5E7EB`

### 1.2 字体与阶梯规范 (Type Scale)
- **Display 56rpx (28px)**：BMI 大数字、日历头部日期数字、闹钟时间。
- **H1 40rpx (20px)**：名片姓名、模态框主标题。
- **H2 34rpx (17px)**：各 Tab 导航标题、各主要分区标题。
- **H3 30rpx (15px)**：卡片内小标题、药箱药品名、病历医院名。
- **Body 30rpx (15px)**：表单输入框文本、AI 关怀正文、动态内容。
- **Caption 26rpx (13px)**：规格标签、时间戳、次要辅助提示。
- **Overline 22rpx (11px)**：角标徽标、状态小标签、小图例。

---

## 二、全局数据模型与字段字典 (Data Schema)

### 2.1 家庭成员模型 `HealthProfile` (`MemberProfile`)
| 字段名 | 类型 | 必填 | 默认值 | 描述 |
| :--- | :--- | :--- | :--- | :--- |
| `id` | `string` | 是 | 自动生成 | 成员唯一 ID，如 `p_father` |
| `userId` | `string` | 否 | `u_admin` | 所属管理者用户 ID |
| `name` | `string` | 是 | 无 | 成员真实姓名/称谓 (如 "张建国 (父亲)") |
| `relation` | `enum` | 是 | `'father'` | 关系: `'self'` \| `'father'` \| `'mother'` \| `'spouse'` \| `'child'` \| `'other'` |
| `role` | `enum` | 是 | `'cared'` | 角色: `'manager'` (管理者) \| `'cared'` (被关爱者) |
| `gender` | `enum` | 是 | `'male'` | 性别: `'male'` (男) \| `'female'` (女) |
| `age` | `number` | 是 | `65` | 年龄 (周岁) |
| `height` | `number` | 否 | `170` | 身高 (cm) |
| `weight` | `number` | 否 | `68` | 体重 (kg) |
| `bmi` | `number` | 否 | 自动计算 | 体质指数 $BMI = weight / (height/100)^2$ |
| `avatarUrl` | `string` | 否 | 预设头像 | 头像图片 URL 或 Base64 图片 |
| `avatarColor` | `string` | 否 | `'#0D9488'` | 成员专属色系 |
| `tags` | `HealthTag[]`| 否 | `[]` | 绑定的健康警示标签列表 |

### 2.2 用药计划模型 `Medication`
| 字段名 | 类型 | 必填 | 默认值 | 描述 |
| :--- | :--- | :--- | :--- | :--- |
| `id` | `string` | 是 | 自动生成 | 计划唯一 ID，如 `med_1725200000` |
| `profileId` | `string` | 是 | 无 | 归属家庭成员 ID (`'all'` 模式下通过此字段关联) |
| `name` | `string` | 是 | 无 | 药品商品名称 (如 "拜新同 硝苯地平控释片") |
| `commonName` | `string` | 否 | `""` | 药品通用名 (如 "硝苯地平控释片") |
| `specification`| `string` | 否 | `""` | 规格包装 (如 "30mg*7片/盒") |
| `imageUrl` | `string` | 否 | `""` | 药盒包装图片 URL |
| `dosage` | `string` | 是 | `"1片"` | 复合剂量文本，如 "1片" / "10mg" |
| `dosageValue` | `number` | 否 | `1` | 单次剂量数值 |
| `dosageUnit` | `string` | 否 | `'片'` | 单次剂量单位: 片/粒/mg/g/ml/包/滴/喷/IU |
| `administrationRoute` | `string` | 否 | `'口服'` | 给药途径: 口服/嚼服/含服/外用/滴眼/喷雾/皮下注射等 |
| `deliveryMethod` | `string` | 否 | `'温水送服'` | 送服方式: 温水送服/足量水(≥250ml)/随餐同服等 |
| `mealTiming` | `string` | 否 | `'after_meal'` | 服药时机: `before_meal` \| `after_meal` \| `with_meal` \| `empty` \| `bedtime` \| `none` |
| `mealTimingLabel`| `string` | 否 | `'饭后 15–30 分钟'` | 服药时机用户端展示标签 |
| `precautions` | `string[]`| 否 | `['忌酒']` | 禁忌与注意事项标签清单 |
| `missedDoseAction` | `string` | 否 | 默认提示 | 漏服处理措施说明 |
| `frequencyType` | `string` | 否 | `'once_daily'` | 频次类型: `once_daily` \| `twice_daily` \| `thrice_daily` \| `custom` |
| `frequency` | `number` | 是 | `1` | 每日服用次数 |
| `interval` | `number` | 否 | `4` | 安全服药间隔时长 (小时，默认4小时) |
| `scheduleTimes`| `string[]`| 是 | `['08:00']` | 每日各次服用时间点数组 (HH:mm) |
| `startDate` | `string` | 是 | 当日 | 计划生效起始日期 (YYYY-MM-DD) |
| `endDate` | `string` | 否 | `""` | 计划结束日期 (YYYY-MM-DD，长期服用为空) |
| `isLongTerm` | `boolean` | 是 | `true` | 是否为长期用药 |
| `alarmEnabled` | `boolean` | 是 | `true` | 是否开启到点智能闹钟强提醒 |
| `stock` | `number` | 否 | `30` | 关联药箱当前库存余量 |
| `stockUnit` | `string` | 否 | `'片'` | 库存余量单位 |
| `stockAlertDays`| `number` | 否 | `7` | 低库存告警阈值 (天) |
| `stockDays` | `number` | 否 | 自动测算 | 按当前频次测算的预计可用天数 |
| `isInCabinetOnly` | `boolean` | 否 | `false` | 若为 true 则仅作为药箱储备，不排程日历 |
| `reminderText` | `string` | 否 | `""` | 语音播报合成文案 |

### 2.3 药箱药品模型 `MedicineCabinetItem`
| 字段名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `id` | `string` | 是 | 药箱记录唯一 ID |
| `userId` | `string` | 是 | 管理者 ID |
| `memberId` | `string` | 否 | 专属于某个成员 (为空则为全家公用药箱) |
| `name` | `string` | 是 | 药品名称 |
| `commonName` | `string` | 否 | 药品通用名称 |
| `specification`| `string` | 否 | 规格包装 (如 0.5g*24粒/盒) |
| `form` | `string` | 否 | 剂型: 控释片/胶囊/颗粒/软膏/溶液等 |
| `stock` | `number` | 是 | 当前库存数量 (如 3) |
| `stockUnit` | `string` | 是 | 库存单位 (如 "盒" / "瓶" / "板") |
| `stockAlertDays`| `number` | 是 | 预警天数阈值 (默认 7 天) |
| `location` | `string` | 否 | 存放物理位置 (如 "客厅电视柜下层药箱") |
| `expireDate` | `string` | 否 | 有效期截止日期 (YYYY-MM-DD) |
| `productionDate`| `string` | 否 | 生产日期 (YYYY-MM-DD) |
| `manufacturer` | `string` | 否 | 生产厂家名称 |
| `approvalNumber`| `string` | 否 | 批准文号 (如 "国药准字H20000000") |
| `batchNumber` | `string` | 否 | 生产批号 |
| `imageUrl` | `string` | 否 | 药品包装实拍图 URL |
| `precautions` | `string[]`| 否 | 注意事项 |
| `linkedMedicationId`| `string` | 否 | 关联的正在执行的用药计划 ID (若无则显示「未设置提醒」) |

### 2.4 服药打卡流水记录 `MedicationLog`
| 字段名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `id` | `string` | 是 | 打卡记录 ID |
| `medicationId` | `string` | 是 | 关联用药计划 ID |
| `medicationName`| `string` | 是 | 药品名称快照 |
| `profileId` | `string` | 是 | 所属成员 ID |
| `date` | `string` | 是 | 打卡计划日期 (YYYY-MM-DD) |
| `doseIndex` | `number` | 是 | 当日第几次剂量 (0-indexed: 0=第1次, 1=第2次) |
| `scheduledTime`| `string` | 否 | 计划服药时间点 (HH:mm) |
| `taken` | `boolean` | 是 | 是否已服用 (`true` / `false`) |
| `operatorId` | `string` | 否 | 实际操作打卡人 ID |
| `operatorName` | `string` | 否 | 打卡人称谓: "本人打卡" 或 "家人代打卡" |
| `timestamp` | `string` | 是 | 实际打卡精确时间戳 (YYYY-MM-DD HH:mm:ss) |
| `status` | `enum` | 是 | `'taken'` (已服) \| `'missed'` (漏服) \| `'pending'` (待服) \| `'skipped'` (跳过) |
| `note` | `string` | 否 | 补卡/修改备注 (如 "早餐后半小时已服") |

### 2.5 综合病历档案模型 `MedicalRecord`
| 字段名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `id` | `string` | 是 | 病历记录 ID |
| `memberId` | `string` | 是 | 所属成员 ID |
| `templateType` | `enum` | 是 | 模版类型: `doctor_diagnosis` (门诊诊断) \| `discharge_summary` (出院小结) \| `recheck_report` (专科复查) \| `health_checkup` (体检报告) \| `emergency_visit` (急诊记录) \| `chronic_followup` (慢病随访) \| `daily_health_log` (日常记录) |
| `title` | `string` | 是 | 病历/报告标题 |
| `date` | `string` | 是 | 就诊/报告日期 (YYYY-MM-DD) |
| `hospital` | `string` | 否 | 就诊医院/体检机构全称 |
| `department` | `string` | 否 | 就诊科室 (心血管内科/内分泌科等) |
| **医生档案字段** | | | |
| `doctor` | `string` | 否 | 接诊医生姓名 |
| `doctorTitle` | `string` | 否 | 医生职称 (如 "主任医师"、"知名专家") |
| `doctorAvatarUrl`| `string`| 否 | 医生工作照或胸牌实拍图 |
| `doctorRating` | `number` | 否 | 星级满意度评分 (1 ~ 5) |
| `doctorTags` | `string[]`| 否 | 就医印象标签 (如 ["认真负责", "开药对症", "问诊细致"]) |
| `doctorKeyAdvice`| `string`| 否 | 医生叮嘱的核心一句话备忘 |
| `doctorImpression`| `string`| 否 | 医德医风与综合就医评价 |
| **复查排程字段** | | | |
| `recheckEnabled`| `boolean`| 否 | 是否开启复查提醒排程 (默认 true) |
| `recheckDate` | `string` | 否 | 计划复查日期 (YYYY-MM-DD) |
| `recheckCycleLabel`| `string`| 否 | 周期标签 (如 "1个月后"、"3个月后") |
| `recheckCycleDays`| `number`| 否 | 周期天数 (如 30、90) |
| `recheckItems` | `string[]`| 否 | 复查项目清单 (如 ["空腹抽血", "静息心电图", "动态血压监测"]) |
| `recheckRemindDaysBefore`| `number`| 否| 提前提醒天数 (如 1, 3, 7 天) |
| `recheckFasting`| `boolean`| 否 | 是否需空腹 |
| `recheckPrecautions`| `string`| 否 | 复查注意事项 (如 "复查前一日忌油腻，早晨勿服降糖药") |
| `recheckStatus` | `enum` | 否 | `'pending'` (待复查) \| `'completed'` (已完成复查) |
| **诊疗内容字段** | | | |
| `chiefComplaint`| `string` | 否 | 主诉与主要病情描述 |
| `diagnosis` | `string` | 否 | 临床诊断结论 / 体检综合结论 |
| `doctorAdvice` | `string` | 否 | 治疗医嘱 / 出院后指导 |
| `abnormalItems` | `string[]`| 否 | 核心异常指标清单 |
| `images` | `string[]`| 否 | 病历/处方/化验单照片列表 (多张) |
| `pdfUrl` | `string` | 否 | PDF 检查报告文件地址 |
| `attachments` | `Attachment[]`| 否| 结构化附件列表 (含文件名、大小、上传时间) |

---

## 三、全局公共组件与框架交互规范

### 3.1 顶部家庭成员切换轨 (MemberSwitcherRail)
- **展示位置**：固定位于首页、动态页、用药页、健康数据页的自定义导航栏下方，背景纯白 `#FFFFFF`，底部带 1px 细分割线。
- **包含元素**：
  1. **「全部 (全家概览)」胶囊**：
     - 未选中：浅灰底色 `#F3F4F6`，文字“全部”，字号 12px。
     - 选中态：翡翠绿底色 `#059669`，白色文字，外层带 `box-shadow: 0 0 10px rgba(5,150,105,0.25)`。
  2. **成员头像项列表 (水平弹性滚动)**：
     - 头像：40px (80rpx) 圆形，外框带 2px 透明间距。
     - 选中态：头像外层带 **3px 翡翠绿 `#059669` 实线边框** + **翡翠绿外发光阴影**。
     - 管理者标识：右上角展示金色皇冠/爱心小角标 (14px)。
     - 姓名文本：头像下方居中，字号 11px；选中时变更为翡翠绿 `#059669` 加粗，未选中为灰色 `#6B7280`。
- **点击交互逻辑**：
  - **点击具体成员**：触发 `setActiveProfileId(profile.id)`。全局各页面实时将数据源过滤为该成员；切换时页面内容执行 150ms 淡出 + 200ms 淡入物理动效。
  - **点击「全部」**：触发 `setActiveProfileId('all')`。全局各页面切换为“全家汇总模式”，卡片右上角或左下角自动呈现所属成员姓名徽标及专属色系圆点。

### 3.2 全局危险操作二次确认对话框 (GlobalConfirmModal / 8.0 规范)
- **触发场景**：所有删除用药计划、删除药箱药品、删除病历档案、删除健康指标、解除家庭成员关系、取消服药打卡。
- **弹窗视觉规范**：
  - 居中模态浮层，背景 `rgba(0,0,0,0.6)` 高斯模糊。
  - 顶部警示图标：红色 `#EF4444` 圆底或黄色 `#F59E0B` 盾牌警告图标。
  - 标题：加粗 15px，如“确认删除用药计划？”、“确认取消本次打卡？”。
  - 说明内容：13px 灰色正文，详细说明不可逆后果。
  - 按钮组：
    - 次按钮：“取消 / 返回”，灰色底色 `#F3F4F6`。
    - 主按钮（危险操作）：**红色纯色底 `#EF4444`**，白色粗体字，文案直接明确动作（如“确认删除”、“确认取消”、“确认解除”）。

---

## 四、Tab 1：健康打卡页 (HomeTab) 详细规格

```
┌────────────────────────────────────────────────────────┐
│ [亲安 Logo] 健康打卡           [长辈模式: 关/开]        │
├────────────────────────────────────────────────────────┤
│ [全部] (张建国-父) (李秀珍-母) (我) [+]                   │ ◄── 成员切换轨
├────────────────────────────────────────────────────────┤
│ ┌────────────────────────────────────────────────────┐ │
│ │ 🟢 今日关怀 · 状态良好 😊                 [🔄 刷新] │ │
│ │ 今日各项身体指标平稳，血压控制良好，保持规律用药。   │ │ ◄── AI 关怀卡片
│ │ [🍱 饮食调理] [🏃 运动建议] [💊 用药指导]             │ │
│ │ [📰 资讯: 高血压防护] [📰 资讯: 老年补钙]            │ │
│ └────────────────────────────────────────────────────┘ │
│                                                        │
│ ┌────────────────────────────────────────────────────┐ │
│ │ 🎉 今日用药已全部完成 (所有计划已打卡)         [X] │ │ ◄── 完成横幅
│ └────────────────────────────────────────────────────┘ │
│                                                        │
│ 27日 星期三 · 今日待服药品                             │
│ ┌────────────────────────────────────────────────────┐ │
│ │ [药盒图] 硝苯地平控释片 (30mg)            [🔊 播报] │ │
│ │ 饭后服用 · 每次 1 片                               │ │
│ │ ┌─────────────┬─────────────┬─────────────┐        │ │
│ │ │ 08:00 (早)  │ 12:30 (中)  │ 19:00 (晚)  │        │ │ ◄── 药卡时间槽
│ │ │  ✓ 已服     │  ⚪ 待服    │  ⚪ 待服    │        │ │
│ │ └─────────────┴─────────────┴─────────────┘        │ │
│ │ 进度: 今日第 1/3 次                  [一键打卡]   │ │
│ └────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────┘
```

### 4.1 页面构成要素
1. **顶部导航栏**：
   - 应用品牌名「亲安 (KinCare)」。
   - 今日日期：「X月X日 星期X」。
   - 长辈模式快捷开关胶囊。
2. **AI 每日健康关怀卡片**：
   - 健康评级大字：良好 (绿色 😊) / 一般 (黄色 😐) / 需关注 (红色 😟)。
   - 综合健康摘要（基于当前成员最近 7 天血压、血糖、服药率智能生成）。
   - 3 大生活调理建议小卡：
     - 🍱 **饮食调理建议**（低盐低脂、饮水量）。
     - 🏃 **运动康复建议**（散步时长、避免剧烈时段）。
     - 💊 **用药指导建议**（服药后静坐、禁忌避开）。
   - 右上角刷新按钮：点击播放旋转动画，调用 `refreshDailyCare` 刷新内容。
3. **今日服药完成横幅**：
   - 触发逻辑：当且仅当所选成员今日所有用药计划的所有时间槽均已完成打卡时渲染。
   - 自动消失：展示 6 秒后平滑高度收缩至 0 并淡出；点击右上角 `×` 立即关闭。
4. **今日服药计划卡片流**：
   - 药品包装图片 (50x50px，若无则展示胶囊图标)。
   - 药品名称、规格、用餐时机标签 (饭前/饭后/随餐/空腹)、单次剂量。
   - 右上角语音播报按钮 `[🔊]`：点击调用 Web Speech API 朗读温馨服药提示语音。
   - 服药时间槽横向轨道：等分展示每日各个时间点（如 `08:00`、`12:30`、`19:00`）。
   - 打卡人标识：已打卡项标注「本人打卡」或「家人代打卡」。

### 4.2 核心交互事件与状态机
- **未打卡时间槽点击**：
  1. **服药安全间隔校验**：计算当前系统时间与该药物上一次打卡时间的差值。
  2. 若时间间隔小于设定值（如 4 小时），系统拦截打卡，弹出**黄色安全警示模态框 (IntervalWarningModal)**：
     - 警示文案：“您在 XX:XX 刚服用过该药物，两次服药间隔仅 X 小时（建议间隔 4 小时），确认现在服药吗？”
     - 按钮：“暂不服用 (取消)” / “确认已到时间，立即服药”。
  3. 确认后完成打卡：时间槽由灰色切换为绿色背景 + 白色勾选 `✓`，伴随 300ms 弹性缩放动画与轻度触觉震动；药箱对应库存数量自动扣减单次剂量；进度文本更新为“今日第 X/Y 次”。
- **已打卡时间槽再次点击 (取消打卡)**：
  1. 触发 `GlobalConfirmModal` 确认：“确认取消本次打卡记录？”。
  2. 确认后时间槽回退为灰色未打卡状态，药箱库存自动回滚，系统弹出轻提示 Toast：“已取消打卡”。

---

## 五、Tab 2：健康时间轴页 (TimelineTab) 详细规格

### 5.1 页面构成与布局
- **时间轴主轴线**：距左侧 30px 处纵向延伸 2px 翡翠绿透明度 20% 轴线。
- **日期节点**：带外发光圆点，右侧标注日期与星期。
- **动态气泡卡片**：
  - 作者头像与昵称、发布时间戳、身体状态色标（如「身体状态良好 (绿色)」/「轻微不适 (黄色)」/「就医就诊中 (蓝色)」）。
  - 文字描述正文（支持展开/收起）。
  - 多媒体展示区：
    - **九宫格图片**：最多 9 张，点击任意一张呼出全屏高清灯箱预览，支持多点缩放。
    - **视频播放器**：展示封面与播放三角遮罩，点击在应用内全屏播放。
    - **语音录音条**：展示声波跳动波形、时长（如 `00:15`）及播放/暂停按钮，点击调用 HTML5 Audio 播放。
  - **AI 智能医学分析看板 (自动分析)**：
    - 展开后展示：系统对动态正文中症状（如“头晕”、“胸闷”）的去诊断化科普分析、生活排查指南及权威参考文献来源。
  - **底部互动栏**：爱心点赞按钮（点击触发 scale 1.3 弹性心跳动画，更新点赞数）、操作菜单 `⋯`（支持编辑动态、删除动态）。

### 5.2 发布健康动态模态框 (PublishModal)
- **呼出入口**：点击右下角浮动动作按钮 (FAB `+`) 或顶部「记录动态」。
- **表单输入项**：
  1. **被记录人选择**：下拉选择家庭成员（默认当前选中成员）。
  2. **身体状态标签**：横向单选 Chip 组（身体状态良好 / 指标平稳正常 / 轻微不适 / 就医就诊中 / 病后康复中 / 用药打卡完毕）。
  3. **动态标题**：选填，如“今日早起晨练与身体反馈”。
  4. **动态内容正文**：多行文本框，最小高度 100px，字数统计（最大 500 字）。
  5. **图片上传**：支持拍照或相册选取（最多 9 张），带删除按钮 `×`。
  6. **视频上传**：支持 1 个短视频上传与本地预览。
  7. **微信式长按语音录音条**：
     - 未录音：展示大麦克风按钮，标注“按住 说话”。
     - 按住状态：按钮放大 1.15 倍，中央弹出动态声波指示器与计时器，上滑超过 40px 显示“松开手指，取消发送”。
     - 松手完成：生成语音条，提供试听与删除重录功能。
- **发布逻辑**：点击「立即发布」，提交后写入时间轴列表顶部，并在后台异步触发 AI 语义分析，生成智能分析小看板。

---

## 六、Tab 3：用药管理页 (MedsTab) 详细规格

### 6.1 用药日历视图 (Calendar)
- **月份控制器**：左箭头 `‹` 上月、居中展示 `YYYY年MM月`、右箭头 `›` 下月。
- **7 列星期网格**：日、一、二、三、四、五、六。
- **日期单元格状态**：
  - **今天**：深绿背景 `#0D9488`，白色数字，圆角满格。
  - **选中日**：浅绿背景 `#CCFBF1`，深绿外边框。
  - **完成度圆点三色规则** (日期数字下方 6px 圆点)：
    - 🟢 **全完成 (绿色)**：当日所有用药计划全部按时打卡。
    - 🟡 **部分完成 (浅绿色/黄色)**：当日有用药计划，部分已服，部分待服。
    - 🔴 **漏服/未完成 (红色)**：当日有计划但已过时段完全未服用。
    - 无计划：不显示圆点。
- **选中日用药清单展开区**：
  - 展开展示该日的所有服药计划与打卡时间槽。
  - 在全家模式下，每个计划卡片左上角明确标注所属成员姓名徽标（如 `[父亲 · 张建国]`）。
  - 支持直接在日历清单中点击打卡或取消打卡。

### 6.2 智能家庭药箱视图 (Cabinet)
- **搜索与筛选栏**：输入框实时按药品名、通用名、生产厂家、批号模糊搜索。
- **药箱药品双列卡片**：
  - 药品实拍图/默认包装。
  - 药品通用名与规格（如 `拜新同 硝苯地平控释片 · 30mg*7片`）。
  - **当前库存进度条**：百分比直观填充，剩余数量与单位（如 `库存: 21片 (可用 21 天)`）。
  - **智能角标**：
    - 🟡 **库存告急**：当预计可用天数 $\le$ 预警阈值（默认 7 天）时右上角显示。
    - 🔴 **已过期**：当有效期截止日早于今日时显示。
    - ⚪ **未设置提醒**：未关联排程计划的常备药品显示该标签，点击卡片直接跳转计划创建页。
- **卡片点击抽屉**：展示完整药品说明（批号、厂家、储存位置、注意事项），并提供「为其设置用药提醒」、「编辑药品」、「删除药品」入口。

### 6.3 服药日志与历史方案视图 (History)
- **两大子 Tab 切换**：
  1. **打卡流水记录 (Records)**：以倒序流水账形式列出每一次服药打卡记录，包含日期时间、药品名、剂量、打卡人（本人/代打卡）、状态（已服/漏服）、备注。支持点击单条呼出修改弹窗。
  2. **历史方案库 (Plans)**：汇总创建过的所有用药计划。每项方案提供「一键复用为新计划」按钮。

### 6.4 用药计划录入/编辑弹窗 (MedicationPlanModal)
- **全屏模态表单结构**：
  1. **快捷载入工具栏**：
     - `[📷 拍照识别/OCR]`：拍照上传处方单，自动提取药品名、剂量、频次。
     - `[📦 从药箱快速选取]`：拉起药箱抽屉，选中药品一键将药名、规格、包装图、库存带入表单。
     - `[📋 载入历史方案]`：拉起既往方案库，一键填充全部参数。
  2. **药品核心字段**：
     - 药品商品名称 (必填)
     - 药品通用名称 (选填)
     - 规格包装 (如 `10mg*30粒/盒`)
     - 药品包装图片上传与预览
  3. **用药周期与生效时间**：
     - **长期用药开关**：开启时结束日期自动隐藏并标记为长期；关闭时显示结束日期选择器。
     - 开始日期选择器 (默认今日)
     - 快捷周期 Chip：`[7天]` `[14天]` `[30天]` `[长期]`
  4. **单次剂量三联动输入组件**：
     - 方式一：数字键盘精确输入数值 (如 `1`、`0.5`、`20`)。
     - 方式二：步进器 `[−]` `[+]`，支持长按连续增减。
     - 方式三：单位选择器下拉 (片 / 粒 / mg / g / ml / 包 / 滴 / 喷 / IU)。
  5. **给药途径与送服方式**：
     - 给药途径单选 Chip：口服 / 嚼服 / 含服 / 冲服 / 外用 / 滴眼 / 喷雾 / 雾化 + `[+ 自定义]`。
     - 送服方式单选 Chip：温水送服 / 足量水(≥250ml) / 直接吞服 / 随餐同服 / 睡前吞服 + `[+ 自定义]`。
  6. **服用时机单选**：
     - 饭前30分钟 / 饭后15-30分钟 / 随餐 / 空腹 / 睡前 / 无特殊要求。
  7. **禁忌与注意事项 (多选 Chip 组)**：
     - 预设：忌酒 / 忌与牛奶同服 / 忌与钙铁剂同服 / 忌与西柚同服 / 不可嚼碎(缓释肠溶) / 服药后不宜驾车 / 需避光保存 / 需冷藏(2-8℃) / 多喝水。
     - 支持点击 `[+ 自定义]` 追加自定义禁忌标签，支持长按删除标签。
  8. **用药频次与每日时间点排程**：
     - 频次类型：每日1次 / 每日2次 / 每日3次 / 每日4次 / 隔日一次 / 每周指定天 / 按需服用(PRN)。
     - **时间点动态列表**：根据频次自动生成对应时间选择器 (如 `08:00`、`12:30`、`18:00`)，支持点击添加或删除时间点。
     - **差异化剂量开关**：开启后，可单独为每个时间点配置不同剂量（如早晨1片、晚间2片）。
  9. **到点强提醒闹钟开关**：
     - 智能闹钟开关 (默认开启)：到点调用系统通知与内置多通道蜂鸣+语音播报。
  10. **保存机制与药箱兜底**：
      - 点击「保存计划」：校验药品名称必填项；自动写入用药计划数据库；若勾选“同步加入药箱”，自动检查药箱并同步新建或更新库存；立即将该计划排程至月度日历中。

### 6.5 药箱药品录入/编辑弹窗 (MedicineCabinetModal)
- 包含字段：药品名称、通用名称、生产厂家、批准文号、生产批号、规格、剂型 (片剂/胶囊/颗粒/注射液等)、当前库存数量、库存单位、库存预警阈值天数、存放物理位置、储存条件 (如阴凉避光)、有效期截止日期、生产日期、适应症、条形码扫码录入、药品实拍图上传。

### 6.6 到点服药测试闹钟仿真弹窗 (TestAlarmModal)
- **触发入口**：用药页右上角「🔔 测试闹钟」。
- **仿真效果**：
  - 全屏高亮弹窗，带有红绿交替呼吸发光边框与脉冲波纹。
  - 调用 Web Audio API 合成真实电子闹钟持续蜂鸣警报音（哔-哔-哔-哔）。
  - 调用 Web Speech API 朗读语音提示词：“该吃药啦！张建国请服用硝苯地平控释片，每次1片，饭后温水送服。”
  - 界面提供两个大按钮：
    - `[🟢 立即服药打卡]`：模拟完成打卡并停止闹铃。
    - `[⏰ 稍后 5 分钟再提醒]`：停止当前闹钟并排程 5 分钟后二次提醒。

### 6.7 服药打卡记录补录/修改弹窗 (MedicationLogEditModal)
- 支持对历史某次打卡记录修改状态 (已服 / 漏服 / 跳过)、修改实际服药时间戳、修改服药剂量、录入补卡原因或症状备注。

---

## 七、Tab 4：健康数据全景页 (HealthTab) 详细规格

### 7.1 健康名片与 BMI 智能圆环
- **名片容器**：翡翠绿到青色 135° 渐变质感大卡片，外层圆角 24px。
- **左侧个人信息**：成员头像（白边框）、姓名（如“张建国”）、年龄与称谓标签（如“68岁 · 父亲 · 男”）、基于近 7 天数据的健康评级标签（良好/一般/需关注）。
- **右侧 SVG 动态 BMI 圆环**：
  - 圆环半径 80px，根据当前 BMI 数值实时计算圆环进度弧度。
  - 中心展示当前 BMI 大数字（如 `23.5`）与分类标签（`BMI 正常` / `偏瘦` / `超重` / `肥胖`）。
  - **点击 BMI 圆环**：底部滑出快速录入抽屉，支持数字键盘修改身高 (cm) 与体重 (kg)，保存后实时重新计算 BMI 并刷新圆环。

### 7.2 重点指标概览面板
- 横向等分展示 **血压 (收缩压/舒张压)**、**血糖 (空腹/餐后)**、**心率**、**体重** 4 张指标卡片。
- 每张卡片显示最新测量数值、单位、正常状态色点（绿/黄/红）、上次测量时间戳。
- 点击任意卡片，平滑滚动至下方对应指标的详细趋势图与历史记录。

### 7.3 健康趋势图表 (HealthTrendChart)
- **指标切换器**：横向切换查看「血压走势」、「血糖波动」、「心率监测」、「体重趋势」。
- **时间跨度分段器**：`[7天]` `[30天]` `[90天]`。
- **图表渲染**：
  - 采用平滑贝塞尔曲线或高精度折线。
  - 血压图表绘制双折线（收缩压高压红线 + 舒张压低压蓝线），附带正常范围阈值参考带（90~140 / 60~90 mmHg）。
  - 血糖图表按空腹与餐后区分点位标注。
  - 点击折线上的数据节点，弹出 Tooltip 显示具体测量时间、测量情境（如“晨起静息”、“餐后2小时”）及备注。

### 7.4 7 大分类人体全景指标折叠库
包含分类：
1. 🩺 **血压测量** (收缩压、舒张压)
2. 🩸 **血糖监测** (空腹血糖、餐后2小时血糖、睡前血糖、随机血糖、糖化血红蛋白 HbA1c)
3. 💓 **心率与循环** (静息心率、运动心率)
4. ⚖️ **体重与体脂** (体重、身高、BMI、体脂率)
5. 🌡️ **体温与血氧** (腋下体温、血氧饱和度 SpO2、呼吸频率)
6. 🧪 **血脂与代谢** (总胆固醇、甘油三酯、低密度脂蛋白 LDL-C、高密度脂蛋白 HDL-C、尿酸)
7. 📏 **身体形态与生化** (腰围、骨密度 T值、血肌酐、谷丙转氨酶 ALT、肾小球滤过率 eGFR)

- **折叠交互**：各分类卡片默认展示最新一条记录与记录总数，点击卡片展开该分类下的所有历史测量流水。
- **每条流水操作**：展示数值、测量时间、测量情境；点击可呼出编辑修改弹窗，支持单条删除（带二次确认）。
- **组内快捷录入**：点击分类右上角「+ 记一笔」，唤起指标快速录入弹窗并默认选定该指标类型。

### 7.5 指标录入与修改弹窗 (HealthLogModal / EditHealthRecordModal)
- **录入表单字段**：
  - 指标大类与细分类型选择（如血压、空腹血糖、餐后血糖等）
  - 测量数值输入（血压提供高压与低压双输入框）
  - 测量日期与时间选择器（精确到分钟，默认当前系统时间）
  - 测量情境单选 Chip：晨起静息 / 早餐后2小时 / 午餐前 / 午餐后2小时 / 晚餐前 / 晚餐后2小时 / 睡前安静 / 运动后 / 身体不适时
  - 异常参考范围智能判定提示（输入数值后即时提示：“该数值处于正常参考范围内” 或 “偏高，请关注”）
  - 备注文本框（如“服药前自测，测量时情绪略紧张”）

### 7.6 多维指标高级筛选抽屉
- 支持按时间范围（近7天/30天/90天/全部）、指标大类、健康状态（全部/正常/预警/异常）、测量情境进行多维组合交集过滤，筛选结果实时响应更新。

### 7.7 健康标签云系统
- 展示成员专属健康标签（如 `晨峰血压警惕 (红)`、`痛风低嘌呤饮食 (黄)`、`青霉素过敏 (红)`、`防跌倒看护 (蓝)`）。
- **「✨ AI 一键建议标签」**：点击调用 AI 模型，自动综合该成员的既往病历与用药计划，智能生成针对性的健康标签并自动去重追加。
- **自定义添加标签**：支持手动输入标签文本并指定危险级别（红/黄/蓝/绿）。

---

### 7.8 综合病历档案系统 (MedicalRecordModal - 6大模板自适应)

为解决长辈常年就医诊疗资料繁杂、容易遗忘医生姓名及复查周期的痛点，系统提供多模版自适应病历档案管理：

#### 7.8.1 单弹窗 6 大模板下拉动态自适应切换
在弹窗顶部提供模版下拉选择器，切换模板时表单输入字段、占位提示与示例数据将**实时无缝自适应重构**：

| 模板类型 (`templateType`) | 模板名称 | 专属字段与占位提示特征 | 建议复查周期与推荐检查项目 |
| :--- | :--- | :--- | :--- |
| `doctor_diagnosis` | **门诊常规诊断病历** | 主诉与现病史、临床诊断结论、门诊处方与治疗医嘱 | 30天后 (1个月后)；建议项目: 血压监测表复核、生化常规、心电图 |
| `discharge_summary` | **住院与出院小结** | 入院主要病程、手术/处置记录、出院诊断结论、带药医嘱与康复禁忌 | 14天后 (2周后)；建议项目: 血小板聚集率、心肌酶谱、心脏彩超 |
| `recheck_report` | **专科复查与化验对比** | 复查前近期状况、阶段性复查对比评估、用药方案调整说明 | 90天后 (3个月后)；建议项目: 肝肾功能、糖化血红蛋白、颈动脉超声 |
| `health_checkup` | **年度健康体检报告** | 体检综述、核心异常指标清单高亮、检后健康生活指导 | 180天后 (半年后)；建议项目: 甲状腺彩超、血脂四项、腹部超声 |
| `emergency_visit` | **急诊就医处置记录** | 急诊突发起病状况、急诊处置与检查排查、离院观察医嘱 | 7天后 (1周后)；建议项目: 专科门诊复诊随访 |
| `chronic_followup` | **慢病随访与自测评估**| 慢病自测数据汇总、慢病控制综合评价、下一阶段控制目标 | 90天后 (3个月后)；建议项目: 糖化血红蛋白、尿微量白蛋白 |

#### 7.8.2 深度医生档案与就医印象
- **医院与科室**：就诊医院名称、就诊科室（心内科、内分泌科等）。
- **主治医生信息**：医生姓名、职称（主任医师/副主任医师/专家）。
- **医生工作照片上传**：支持拍照或上传医生相片/胸牌，防止长辈多年后淡忘医生样貌。
- **1~5 星就医满意度评价**：支持点击星星打分。
- **就医印象标签 (多选与自定义)**：
  - 预设标签：`认真负责` `问诊细致` `开药对症` `经验丰富` `讲解耐心通俗` `不乱开大检查`。
  - 支持点击 `[+ 自定义]` 添加个性化印象标签。
- **医生叮嘱核心一句话备忘**：单行重点醒目输入框（如：“早晨服药后静坐10分钟，若收缩压低于100立即减半”）。

#### 7.8.3 复查周期自动排程与医疗闹钟
- **复查周期快捷 Chip**：`[1周后 (7天)]` `[2周后 (14天)]` `[1个月后 (30天)]` `[3个月后 (90天)]` `[半年后 (180天)]` `[1年后 (365天)]`。点击自动推算并填充精准复查日期。
- **精准复查日期选择器**：支持手动选取任意未来日期。
- **复查检查项目清单 (Tags 组)**：记录下次复查需要开具的检查单（如 `空腹抽血`、`24小时动态心电图`、`胸部CT`），支持输入后回车追加与点击删除。
- **空腹要求开关**：勾选后在复查提醒中高亮提示“需空腹”。
- **复查注意事项**：多行文本输入（如“提前一天清淡饮食，早晨勿服降糖药”）。
- **复查闹钟提醒开关**：勾选后在临近复查日前 1~3 天通过系统通知与应用内横幅强提醒。

#### 7.8.4 附件多模态上传与管理
- **病历/处方/化验单照片**：支持多张照片上传，展示缩略图列表与单张删除按钮。
- **PDF 电子检查报告**：支持上传 `.pdf`、`.doc` 检验单据，自动解析文件名与文件大小，支持在线点击预览查看。

---

### 7.9 病历高保真详情与复查打卡抽屉 (MedicalRecordDetailModal)
- **展示内容**：
  - 顶部科室大徽标、就诊医院、就诊科室、就医日期、模板类型徽标。
  - **就诊医生档案卡片**：展示医生照片（圆形头像）、姓名、职称、1~5 星评价、就医印象标签及医生核心叮嘱备忘卡。
  - 主诉与现病史、诊断结论（大字高亮展示）、治疗医嘱与用药指导。
  - 异常指标清单高亮列表。
  - 照片九宫格（点击任意一张进入全屏多点触控缩放查看）。
  - PDF 报告附件条（点击调用系统查看器或浏览器新标签页打开）。
  - **复查倒计时与打卡看板**：
    - 展示计划复查日期与当前倒计时（如“距离复查还剩 12 天”）。
    - 列出本次复查需要进行的检查项目清单。
    - **复查打卡按钮**：支持一键在「待复查」与「已完成复查」之间切换。
- **操作按钮**：
  - `[✏️ 编辑病历]`：关闭详情抽屉并拉起 `MedicalRecordModal` 带入当前全部数据进行修改。
  - `[🗑️ 删除病历]`：触发 `GlobalConfirmModal` 确认后物理删除。

---

## 八、Tab 5：个人与系统设置页 (MyTab) 详细规格

### 8.1 用户信息与编辑
- 展示当前登录管理员姓名、手机号（掩码）、专属管理员徽标。
- 点击「编辑个人资料」：弹出弹窗修改管理者姓名与手机号。

### 8.2 家庭成员全权管理与编辑 (MemberEditModal)
- **成员列表卡片**：
  - 列出家庭所有被关爱成员，展示头像、姓名、年龄、关系（父亲/母亲/配偶/子女等）、身体健康标签。
  - 右侧提供 `[编辑]` 按钮与 `[解除/删除]` 按钮。
- **添加/编辑家庭成员弹窗 (MemberEditModal)**：
  - 成员姓名 (必填)
  - 与我的关系：自己 / 父亲 / 母亲 / 配偶 / 子女 / 其他
  - 性别：男 / 女
  - 年龄 (岁)
  - 初始身高 (cm) 与 初始体重 (kg)
  - **头像设置**：支持从预置卡通长辈头像库一键选取，或点击上传本地实拍照片。
- **解除/删除成员**：触发 `GlobalConfirmModal`，确认后物理删除该成员及其名下的所有用药计划、打卡记录、健康指标与病历档案。

### 8.3 AI 服务连接配置与连通性测试
- **AI 服务提供商选择器**：OpenAI / 阿里云百炼 (通义千问) / DeepSeek / Google Gemini / 自定义 API (Custom)。
- **API Key 输入框**：密码形式，支持一键粘贴。
- **Model 模型名称**：根据提供商自动推荐（如 `gpt-4o`、`qwen-max`、`deepseek-chat`），支持自定义修改。
- **Base URL 输入框**：选填，用于自定义中转代理或私有化部署。
- **TTS 语音合成声线**：Kore (温柔女声) / Zephyr (沉稳男声) / 机械女声 / 系统原生。
- **「测试连接」按钮**：点击向配置的 API 发送连通性握手测试，成功显示绿色勾选并提示响应延迟；失败自动无缝启用内置高拟真度离线 Mock 引擎。

### 8.4 语音播报调试引擎
- 提供测试文本输入框（默认“该吃药啦！请按时服用降压药。”），点击「试听播放」调用 Web Speech API 实时测试当前设备的语音播报音量与语速。

### 8.5 全量健康数据备份与导出 (JSON / CSV)
- **导出 JSON 格式**：一键将全家所有成员资料、服药计划、历史打卡日志、所有健康测量指标、全部病历档案打包为标准 JSON 备份文件，自动触发浏览器下载。
- **导出 CSV 格式**：将全部健康指标记录导出为 Excel 可读的 `.csv` 表格文件。

### 8.6 多语言、账号安全与退出
- **语言切换**：简体中文 (zh_CN) / 繁体中文 (zh_HK) / English (en_US)。
- **退出登录**：清除本地登录 Token 与会话缓存，安全重定向至登录页。

---

## 九、全局危险操作二次确认机制 (8.0 规范)

为保障高龄用户及家庭健康数据的安全性，杜绝任何误触操作，系统严格遵循以下二次确认准则：

1. **统一拦截规范**：凡涉及 `删除用药计划`、`删除药箱药品`、`删除病历记录`、`删除健康测量流水`、`解除/删除家庭成员`、`撤销服药打卡` 等破坏性操作，**严禁直接执行，严禁仅以 Toast 代替**。
2. **必须呼出 `GlobalConfirmModal`**：
   - 必须向用户明确展示即将删除的实体名称（如“确定删除「拜新同 硝苯地平控释片」用药计划吗？”）。
   - 必须明确说明操作后果（如“删除后该药品的历史打卡记录将同步解除，且不可恢复”）。
   - 主操作按钮必须采用明确动作语义且使用**红色警示色 `#EF4444`**（如“确认删除”、“确认解除”）。

---

## 十、长辈适老模式 (Senior Mode) 渲染规范

当用户开启「长辈模式」时，全局 CSS 变量与组件层自适应执行以下适老化规则：

| 界面维度 | 标准常规模式 | 长辈适老模式 (Senior Mode) |
| :--- | :--- | :--- |
| **正文字号 (Body)** | 14px (28rpx) ~ 15px (30rpx) | **18px (36rpx) ~ 20px (40rpx)** |
| **标题字号 (H2/H3)**| 16px (32rpx) ~ 18px (36rpx) | **22px (44rpx) ~ 24px (48rpx)** |
| **触控热区高度** | 36px ~ 44px (72rpx ~ 88rpx) | **$\ge$ 52px (104rpx)** |
| **色彩与对比度** | 追求轻量层次感，包含浅灰辅助字 | **对比度强制 $\ge 4.5:1$，状态必须配文字说明 (非仅靠颜色圆点)** |
| **列表卡片间距** | 8px ~ 12px | **16px ~ 20px** |
| **按钮与开关** | 标准 32px 宽度开关 | **放大 1.4 倍，增加明晰的「开 / 关」中文字样** |
| **到点提醒** | 基础横幅提示 | **自动触发系统大字弹窗 + 强震动 + 自动朗读语音播报** |

---
*本说明书完整覆盖了亲安系统的全部功能模块、界面视觉结构、表单字段、状态流转与交互事件，可作为全栈开发、UI 还原与功能验收的唯一准则。*
